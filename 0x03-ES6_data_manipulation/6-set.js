/**
 * setFromArray - Creates a Set from an array.
 * @param {Array} arr - The input array from which to create a Set.
 * @returns {Set} - A Set containing unique elements from the array.
 */
const setFromArray = (arr) => {
  return new Set(arr);
};

export default setFromArray;
